# Rust Linter Configuration
# Phenotype Standard
#
# Note: clippy.toml only accepts clippy-specific configuration keys.
# Lint level configuration (deny/warn/allow) belongs in Cargo.toml [lints]
# or on the command line via `cargo clippy -- -D warnings`.

too-many-arguments-threshold = 10
